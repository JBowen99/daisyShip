---
import { Icon } from "astro-icon/components";

interface Props {
  size?: number;
  showTitle?: boolean;
}

const { size = 32, showTitle = true } = Astro.props;
---

<div class="flex flex-row items-center">
  <Icon name="daisyShip" class="mr-3" size={size} />{showTitle && "DaisyShip"}
</div>
